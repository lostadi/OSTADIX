# Full-goal audit: completion is not established

## Current implementation and live observations — 17:57–18:08 UTC

The local factory model running in genuine AICore generated a complete
Python/Rust/Bash `.O` program. A controlled host invoked the existing canonical
primary `o_execute`, received a typed result of **26 units: north 13 / south 13;
IDs 2, 3, 8, 13**, and provided that actual result to the model. The model's
subsequent natural-language answer matched it. Native O execution took **802 ms**;
the model result-consumption attempt took **40,869 ms including loading and
cleanup**. See the [current status](../gemini-nano-rebuild-20260917/STATUS.md),
[actual source/tool result](../gemini-nano-rebuild-20260917/nano-program-mcp-result.json),
[acceptance check](../gemini-nano-rebuild-20260917/nano-program-acceptance.json),
and [model consumption](../gemini-nano-rebuild-20260917/canonical-nano-result.json).

This required **four program generations and two model repair rounds**: raw empty
output, a chat-framed program failing with `AttributeError`, repair 1 failing
with `IndentationError`, and repair 2 executing but returning its closing Markdown
fence as the final O text. The host then removed exactly the outer fence while
preserving the program body. This is disclosed host orchestration, not autonomous
model function calling or an ordinary Gemini UI invocation.

Submitted source identity:
`58f4f49e8fa4a7481800dafe158564e67f30fc18a6bc5d36c4313ba67765fe0a`.
Typed result identity:
`7a06fec778a8fdf1970f466793a268c660c0bb3238ffb35844b83e7a1e33f193`.
The actual native response also retains V6 admission and OIR/plan/graph identities;
these are descriptive evidence, not signed caller attestations.

The canonical UID 10402 host's
[18:00 execution test](../gemini-nano-rebuild-20260917/nano-program-canonical-broker-result.json)
failed with `expected absolute path: "--o-backend"` and native exit 1. That
historical failure was reproduced and repaired by disabling the Termux linker
wrapper in the verified **`u:r:ksu:s0`** context; see the
[loader diagnostic](../gemini-nano-rebuild-20260917/appfunction-host-loader-diagnostic.json).
After restart as PID **9901**, the
[18:08 controlled HTTPS retest](../gemini-nano-rebuild-20260917/nano-program-canonical-broker-fixed-result.json)
returned HTTP 200, `isError: false`, completed native execution in **787 ms**,
and the same full-program typed result and source/result identities. This is a
successful controlled loopback transport test under KernelSU, with no real
Android AppFunction or ordinary Gemini caller demonstrated.

Exact public Nano release identity, public Prompt API availability and the
ordinary assistant-to-tool connection remain unverified. The earlier loading
and readiness blockers below are retained as historical observations.

**Earlier reconstruction update — historical:** the stock local TrustyDecrypt
service had decoded the factory metadata and 191 payloads had passed manifest
size/hash checks. This superseded the earlier conclusion below that no usable local
bootstrap was found. That native load failed requesting an EdgeTPU device
descriptor; no inference ran in that test. See the
[factory reconstruction audit](../gemini-nano-rebuild-20260917/FACTORY-FORMAT-RECOVERY.md)
and `../gemini-nano-rebuild-20260917/local-model-load-readonly.stderr`.

The user explicitly requires Gemini Nano and an ordinary Pixel assistant
invocation. A separate probe, a fixed ASI selector, unidentified Gemini model,
or directly invoked Android broker does not satisfy that final requirement.

## Implemented work and evidence scope

| Requirement | Evidence inspected | Assessment |
|---|---|---|
| Original ASI method delegated at most once, including exceptions | `AsiDelegation.java`: the sole `original.invoke` is outside the preparation catch; `AicoreOstadixModule.java` uses `PASSTHROUGH`; retained corrected build/self-test log | Implemented and tested in the recorded helper cases |
| Corrected provenance build installed and controlled live request repeated | `../gemini-ostadix-20260917/live/asi-corrected-correlated.txt`, installed APK SHA-256 `bbb12fd906787e6720a74d3634f95f2346da1864f838662aa7fd1cbd8f8e64a5`, freshly rechecked | Demonstrated: two selected/forwarded requests; framework callback is time-correlated, not a signed shared-request receipt |
| Inspect existing MCP schema before changes and reuse one source-first tool | Earlier advertised schema; canonical `mcp/ostadix_lang_mcp_server/src/{main,unified}.rs` | Existing primary `o_execute` retained; complete source alone suffices; newer source/path/actions/jobs/mesh interfaces preserved |
| Ordinary heterogeneous program, actual result and evidence | `../source-first-mcp-20260917/source-and-lifted-final-exchange.json` and `examples/ai_inventory_one_call.O` | Python, Rust and Bash composed to return total 50; source generated by this coding assistant, not Nano |
| Existing lifted-project path through the same operation | Same final exchange | Demonstrated total 42 with the existing project executor; its evidence fields are not represented as ordinary-runtime identities |
| Host limits, cancellation, precise failures and no silent repetition | Current handler, `lifted-lifecycle.stdout`, `lifted-lifecycle-exchange.json` and Android broker acceptance | Recorded cancellation/deadline/failure cases dispatch once, clean workspaces, prevent delayed descendant writes and recover; this is not a general security-containment claim |
| Android adapter reaches that same primary tool | `../gemini-ostadix-20260917/live/appfn-mcp-acceptance.json` | Demonstrated through direct test-harness requests; not a Nano/Gemini caller |
| Any claimed successful remote execution tested separately | No successful remote execution is claimed | Not established; no remote success may be reported |
| Model-generated heterogeneous program and model consumption of its actual result | `../gemini-nano-rebuild-20260917/nano-program-mcp-result.json`, `canonical-nano-result.json` | **Demonstrated through a controlled host**, with disclosed model repairs and fence extraction |
| Ordinary assistant invokes Nano, Nano calls the tool autonomously, then consumes its result | Bounded assistant-route investigation and current controlled-host caller labels | **Not achieved** |

At the earlier blocked checkpoint, the installed Android adapter SHA-256 was
`0329bb0fcff1f8a83b03aa2af71ba73926811e7d60837e1f7958e74008600f6b`.
The primary MCP binary at that checkpoint was
`c63c8533d0c8714bb83f91eb2f1a1137f16bfa7fd37d43f7ba600a59506a122d`,
matching those historical MCP exchanges. Those checks are retained in
`blocked-audit-current.json`. The current canonical MCP SHA-256 is
`b879c51f6480cf8b1e7a0f1d24a519519f7ae6263ad6a7e5749524b274ce88e6`;
its UID 10402 installation is recorded in
[canonical-mcp-host-install.json](../gemini-nano-rebuild-20260917/canonical-mcp-host-install.json).
That record also captures the scoped loader-fix restart. The initial failed
launch and the subsequent successful controlled HTTPS retest remain separate
observations.

## Historical blocking condition before local reconstruction

1. In the user-directed Nano targeting turn, stable/full returned `UNAVAILABLE`
   and error 606 for feature 636. No inference ran.
2. In the next goal continuation, all four documented configurations returned
   `UNAVAILABLE`, with missing features 636, 645, 647 and 646. The legacy service
   history was checked independently of Android's framework history; both were
   empty. The factory payload inspection found model components without a
   demonstrated complete, usable Nano configuration or feature catalog.
3. In this goal continuation, live readiness still reports zero records in both
   histories, zero matching loaded model mappings, and terminal preload
   `FAILED(3),attempts=12,stop_reason=-256`. No active preload job is being waited
   on. Installed artifact identities still match the recorded evidence.

The exact reason for the prior provisioning server rejection remains unproven.
At that checkpoint the diagnosis was unavailable public Nano features before inference.
The bounded code/artifact reviews then found no evidenced
local repair or usable alternative Nano request path. Subsequent local
reconstruction recovered the metadata and payloads described above. The goal
remains incomplete; the earlier checkpoint does not establish an impasse for
the recovered native path.

## Unverified claims and remaining work

1. Identify and connect the ordinary Pixel assistant query/response boundary to
   the verified local native model path. The existing catalog hook does not
   select Nano. Exact public model identity and public Prompt API readiness still
   need their own evidence; private native success does not imply either.
2. Verify the intended real Android AppFunction/broker caller and request
   correlation if this is the chosen assistant route. The UID 10402 controlled
   HTTPS transport is now working under `u:r:ksu:s0`; other Android SELinux
   contexts and boot persistence remain unverified.
3. Replace the controlled host's manual model/tool/result choreography with the
   intended assistant integration. Preserve fresh model source, any explicit
   transformations, actual primary-tool results and the model's consumption of
   those results in the same assistant interaction.
4. Verify cancellation, request correlation, host authority and precise errors
   across that complete ordinary-assistant chain. Preserve the existing bounded
   ASI experiment and the separately passing MCP lifecycle evidence.

The model-generation/tool-execution/result-consumption components have a working
controlled demonstration. General task reliability, first-attempt correctness,
concurrent runtime overlap and remote placement were not established by this
single shipment fixture.

No cloud model, different local model, or separate demo has been substituted
for this requested completion condition.
