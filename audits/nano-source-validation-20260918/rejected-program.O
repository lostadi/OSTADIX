let parts = autonomous(batch(
python^(
    __oval_result__ = sum([2, 7, 11])
)_python,
rust^(
fn main(){
    let mut accumulator: i32 = 0;
    for i in 1..=4 {
        accumulator += i * i * i;
    }
    println!("{}", accumulator);
}
)_rust,
bash^(
    printf '%s\n' red blue red | sort -u | wc -l
)_bash
))
python^(
    a, b, c = $parts
    __oval_result__ = a + b + c
)
